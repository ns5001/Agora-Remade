
<% if @list %>
<!-- Find out why you need to specify partial over here -->
<%= render partial: 'apartments/apartments', locals: {apartments: @list.apartments}%>
<%= render partial:'comments/comments', locals: {comments: @list.comments}%>
<% end %>
